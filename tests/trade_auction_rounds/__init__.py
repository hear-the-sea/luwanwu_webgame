"""Trade auction round tests split by responsibility.

These modules are intentionally not named `test_*.py` so that pytest won't
collect them directly; `tests/test_trade_auction_rounds.py` remains the
single collection entrypoint for this group.
"""
